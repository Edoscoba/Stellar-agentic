# Rust
target/
Cargo.lock
# ...except the contracts workspace's. soroban-env-host depends on
# ed25519-dalek ">=2.0.0", so an unpinned CI resolve picks up 3.0.0, which it
# does not compile against — the deployable WASM has to build from a
# reproducible dependency set, not whatever crates.io published today.
!contracts/Cargo.lock
# The signing service is a deployable binary, not a library. Its lockfile is
# not optional: the artifact holding the authority to move money must build
# from a dependency set someone reviewed, not from whatever crates.io published
# the morning of the deploy.
!services/signer/Cargo.lock
test_snapshots/

# Node
node_modules/
dist/
.env
.env.local

# Vite
dist-ssr/
*.local

# Turborepo
.turbo/

# Test output
coverage/
playwright-report/
test-results/
blob-report/
.playwright/

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Stellar
.soroban/
identity/

# Generated deployment configs (contract IDs are environment-specific)
deployments/*.json
!deployments/example.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
