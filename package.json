{
  "name": "stellaragent",
  "version": "0.1.0",
  "private": true,
  "description": "AI Agent Payment Rails on Stellar — monorepo",
  "workspaces": [
    "packages/*",
    "dashboard"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
